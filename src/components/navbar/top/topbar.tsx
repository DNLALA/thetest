import "./topbar.css";

const Topbar = () => {
  return (
    <div>
      <div className="top">
        <section className="topList">
          <span>Manage my bookings</span>
        </section>
        <section className="topList">
          <span>Sign up</span>
        </section>
        <section className="topList">
          <span>Travel Agent Login</span>
        </section>
        <section className="topList">
          <span>Contact Us</span>
        </section>
        <section className="topList">
          <span>Contact Us</span>
        </section>
        <section className="phone">
          <span>Français</span>
        </section>
      </div>
    </div>
  );
};

export default Topbar;
